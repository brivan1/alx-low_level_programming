#include "main.h"

/**
 * _abs- prints absolute value of an integer
 * @i: integer
 * Return: 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		i = -i;
	}
	else if (i > 0)
	{
		i = i;
	}
	else
	{
		i = 0;
	}
	return (i);
}
