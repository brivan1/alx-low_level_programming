#include <stdio.h>
#include "main.h"

/**
 * _puts- prints a string
 * @str: a param to put functions
 * Return: 0
 */

void _puts(char *s)
{
	for (; *str != '\0'; str++)
	{
		_putchar(*str);
	}
	_putchar('\n');
}
